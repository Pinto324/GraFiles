import { Routes } from '@angular/router';
import { LoginComponent } from './Modulos/login/login.component'; // Asegúrate de que la ruta sea correcta

export const routes: Routes = [
  /*{ path: 'login', component: LoginComponent }, // Ruta para el componente Login
  { path: '', redirectTo: '/login', pathMatch: 'full' }, // Redirige a /login si la ruta es vacía
  { path: '**', redirectTo: '/login' }, // Redirige a /login para rutas desconocidas
  {path: 'menu-empleado', component: MenuEmpleadoComponent}*/
];